#!/usr/bin/python3
import sys

if __name__ == '__main__':
    arg_arr = sys.argv
    arg_arr_len = len(arg_arr)
    sum = 0

    if arg_array_len > 1:
        for i in range(1, arg_arr_len):
            sum += int(arg_arr[i])

    print(sum)
